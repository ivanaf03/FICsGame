/// @description Inserte aquí la descripción
// Puede escribir su código en este editor
if (place_meeting(x, y, Hitbox2Obj)) {
    // Colisión con un bloque invisible, realiza acciones necesarias
    // Por ejemplo, detener el movimiento del personaje
    velocidad = 0;
}

show_debug_message("After Collision Check");

